# LexNote — Windows 本地英文词汇笔记本

### TL;DR

LexNote 是一款 Windows 本地优先的英文生词笔记工具。用户在任意应用（浏览器、PDF、Word、IDE、聊天软件）中选中英文单词或句子，按下全局热键即可弹出释义卡片，由用户自己配置的 OpenAI 兼容 LLM 生成精准的翻译、音标、词源联想、例句与用法解释，一键存入本地生词库并支持检索、编辑、导出。目标用户是有真实英文输入场景的学习者与技术/职场从业者，他们不满足于词典的机械释义，需要结合上下文的高质量解析。

---

## Goals

### Business Goals

* 上线 8 周内达成 3,000 次安装，其中 40% 完成 LLM 配置并成功查询至少一次。
* 激活用户周留存（W1）不低于 35%，验证「划词即查」是高频刚需而非一次性尝鲜。
* 单次查询端到端可用率（含 LLM 失败重试）达到 98%，建立「解析准确详细」的产品口碑。
* 保持零服务端成本：所有数据本地存储，LLM 调用走用户自有 Key，实现可持续的免费/买断分发模式。
* 平均每位激活用户生词库沉淀 ≥ 50 条，形成迁移成本与长期使用黏性。

### User Goals

* 在任何应用中不打断阅读流，1 秒内获得选中内容的高质量解析。
* 拿到的不只是翻译，而是音标、词性、语境释义、词根/近义辨析、可直接复用的例句。
* 一键收藏，自动保留当时的上下文原句与来源，避免「查了就忘、忘了没记录」。
* 完全掌控数据与模型：数据留在本机，模型和 Prompt 可自定义。
* 生词库可搜索、可批量管理、可导出到 Anki / CSV / Markdown 继续使用。

### Non-Goals

* 不做内建的间隔重复复习系统（SRS）与每日复习队列，V1 通过导出对接 Anki 等成熟工具。
* 不做多端同步、账号体系与云端服务器；不做 macOS / Linux / 移动端。
* 不做真人发音音频抓取与第三方词典授权内容，V1 发音由 Windows 系统 TTS 承担。
* 不做整篇文档翻译、写作润色等通用 AI 助手能力。

---

## User Stories

### персона A：英文文献阅读者（研究生 / 工程师）

* 作为文献阅读者，我想在 PDF 阅读器中选中一个专业术语后立刻看到贴合上下文的解释，以便不切换窗口继续阅读。
* 作为文献阅读者，我想让 LLM 结合整句判断词义（而非给出一堆无关义项），以便准确理解作者意图。
* 作为文献阅读者，我想把生词连同原句一起收藏，以便日后回看时还原语境。

### персона B：职场英语使用者

* 作为职场使用者，我想在邮件或 Slack 中选中一个短语后看到地道用法与正式/非正式的区别，以便写出得体的表达。
* 作为职场使用者，我想收藏 LLM 给出的例句，以便直接改写复用到自己的邮件里。
* 作为职场使用者，我想给生词打标签（如 Meeting、Contract），以便按场景复习。

### персона C：注重隐私与可控性的高级用户

* 作为高级用户，我想填入自己的 BaseURL、API Key 与模型名，以便使用 DeepSeek、Kimi、OpenRouter 或本地网关。
* 作为高级用户，我想编辑解析用的 Prompt 模板，以便让输出结构完全符合我的学习习惯。
* 作为高级用户，我想确认所有数据仅存于本地数据库文件，以便放心处理敏感文本。

### персона D：生词库管理者（同一用户的另一场景）

* 作为学习者，我想按关键词、标签、时间、来源快速筛选生词，以便找到某次阅读中积累的词。
* 作为学习者，我想手动修改 LLM 生成的释义，以便纠正偏差并加入自己的理解。
* 作为学习者，我想导出为 Anki 牌组或 Markdown，以便在既有工作流中继续使用。

---

## Functional Requirements

### 全局取词与浮窗（Priority: P0）

* 全局热键触发：默认 Ctrl+Alt+D，用户可自定义；在任意前台窗口按下即对当前选中文本发起解析。
* 多重取词策略：优先通过 UI Automation / Accessibility 接口读取选区，失败时回退到「模拟复制 + 剪贴板读取」，并在读取后恢复用户原剪贴板内容。
* 上下文抓取：除选中词外，尝试抓取所在句子（前后各约 200 字符）作为 LLM 的上下文输入。
* 浮窗定位：卡片显示在鼠标/选区附近，自动避让屏幕边缘，支持多显示器与高 DPI 缩放。
* 失焦行为：默认点击外部或按 Esc 关闭；支持「钉住」保持常驻。
* 托盘常驻：主程序最小化到系统托盘，开机自启可选。

### LLM 解析引擎（Priority: P0）

* 自定义 Provider 配置：BaseURL、API Key、Model 名称、温度、最大 tokens、超时时间；兼容任意 OpenAI Chat Completions 接口。
* 连接测试：配置页提供「测试连接」按钮，返回可用性、延迟与模型回显。
* 结构化输出：强制 JSON Schema 输出，字段包括 word / lemma（原形）、pos（词性）、ipa_us、ipa_uk、context_meaning（语境义）、senses\[\]（常见义项）、translation、explanation（详细解释）、associations（词根词缀、近义辨析、易混词）、examples\[\]（3 条例句含中文翻译）、collocations（常见搭配）、register（语域，正式/口语/俚语）。
* 流式渲染：支持 SSE 流式返回，先渲染翻译与音标，其余字段逐段填充，降低感知延迟。
* 短语与句子模式：自动识别选中内容为单词、短语还是整句，切换对应 Prompt 模板（句子模式输出翻译 + 句法拆解 + 关键词汇）。
* Prompt 模板编辑器：内置默认模板，允许用户编辑并保存多套模板，提供变量占位符（{{selection}}、{{context}}、{{native_lang}}）。
* 错误处理与重试：网络失败、鉴权失败、JSON 解析失败分别给出明确提示与一键重试；JSON 解析失败时自动降级为纯文本展示。
* 本地缓存：同一词 + 同一模型的结果缓存 30 天，重复查询直接命中，节省 token 与等待时间。

### 发音（Priority: P1）

* 音标展示：同时展示美式与英式 IPA，由 LLM 输出。
* 系统 TTS 朗读：调用 Windows Speech Synthesis 朗读单词与例句，支持选择语音（如 Microsoft Zira / David）、语速调节，离线可用。
* 快捷朗读：卡片内点击喇叭图标或按空格键朗读当前词。

### 生词收藏（Priority: P0）

* 一键收藏：卡片内「加入生词库」按钮 + 快捷键（Ctrl+S / Enter），保存 LLM 返回的全部字段。
* 上下文留存：自动记录原句、来源应用名称与窗口标题、收藏时间。
* 去重合并：已存在的词再次收藏时提示「已在生词库」，可选择更新为最新解析或新增为独立义项。
* 收藏时快速标签：卡片底部提供标签输入框，支持历史标签自动补全。

### 生词库管理（Priority: P0）

* 列表视图：支持表格与卡片两种密度，列含单词、翻译、词性、标签、来源、收藏时间、查询次数。
* 搜索与筛选：全文搜索（词形、释义、例句），按标签、来源应用、时间范围、掌握度标记组合筛选。
* 详情与编辑：所有 LLM 生成字段均可手动编辑，支持添加个人笔记字段。
* 重新解析：对已收藏词可用当前模型重新生成解析，保留旧版本可对比回滚。
* 批量操作：批量打标签、批量删除、批量导出。
* 掌握度标记：简单三态标记（新词 / 熟悉 / 已掌握），仅作筛选用途，不驱动复习算法。
* 导出：CSV、Markdown、Anki 兼容 TSV（可选择导出字段与卡片正反面映射）。
* 导入：支持 CSV 导入已有词表，字段映射可配置。

### 设置与数据（Priority: P1）

* 数据位置可配置：默认 %APPDATA%，可迁移到自定义目录（便于同步盘托管）。
* 备份与恢复：一键导出完整数据库文件，支持自动定时本地备份（保留最近 7 份）。
* 外观：浅色 / 深色 / 跟随系统主题；卡片字号可调。
* 用量统计：本地展示今日/本月查询次数与估算 token 消耗。

---

## User Experience

**Entry Point & First-Time User Experience**

* 用户从官网或 GitHub Release 下载安装包（MSI / 便携版 zip），安装体积目标 < 80 MB。
* 首次启动进入 3 步引导：
  * 第 1 步：配置 LLM。表单含 BaseURL、API Key、Model，附常见服务商预设（OpenAI / DeepSeek / Kimi / OpenRouter / 本地网关）一键填充 BaseURL。必须通过「测试连接」才能进入下一步，失败时给出针对性错误提示（401 鉴权、超时、模型不存在）。
  * 第 2 步：设置全局热键。检测冲突并提示更换；提供「现在试一试」的内置示例文本，用户选中后按热键完成首次成功体验。
  * 第 3 步：说明数据存储位置与隐私边界（本地数据库 + 仅选中文本发送给用户自配模型），提供开机自启与托盘常驻开关。
* 引导完成后主窗口最小化到托盘，显示一次气泡提示「已就绪，在任意应用选中英文按 Ctrl+Alt+D」。

**Core Experience**

* Step 1：用户在任意应用中用鼠标选中英文文本。
  * 无需切换窗口，无需先打开 LexNote 主界面。
  * 选中为空或非英文内容时，按热键后浮窗提示「未检测到选中文本」并在 1.5 秒后自动消失。
* Step 2：按下全局热键，浮窗在选区附近弹出。
  * 100ms 内出现骨架屏，顶部显示已识别的选中词与截取的上下文（可折叠）。
  * 若取词降级为剪贴板方式，需在完成后静默恢复原剪贴板内容，不干扰用户。
* Step 3：LLM 流式返回，卡片分区渲染。
  * 首屏优先区：单词原形、词性、美/英音标 + 朗读按钮、语境释义（最重要的一行，加大字号）。
  * 展开区：详细解释、常见义项列表、联想（词根词缀 / 近义辨析 / 易混词）、3 条例句（英文 + 中文翻译，例句可单独朗读）、常见搭配、语域标注。
  * 流式过程中显示进度指示；超过 15 秒未完成则提示超时并提供重试。
* Step 4：用户决定是否收藏。
  * 点击「加入生词库」或按 Enter，按钮变为「已收藏」并显示撤销入口（5 秒内可撤销）。
  * 收藏时可即时输入标签，回车确认；不输入也可直接保存。
  * 已存在的词展示「已收藏于 3 天前」并提供「更新解析」选项。
* Step 5：用户按 Esc 或点击外部关闭浮窗，回到原应用继续阅读。
  * 整个流程不夺取原应用的输入焦点（浮窗为非激活窗口，仅在交互时获取焦点）。
* Step 6：用户在需要时打开主窗口管理生词库。
  * 左侧为标签与筛选面板，中部为词表，右侧为选中词详情。
  * 详情区所有字段可就地编辑，编辑后自动保存并显示保存状态。
  * 顶部提供搜索框与「导出」入口，导出前可预览字段映射。

**Advanced Features & Edge Cases**

* 无选中内容时按热键：回退为弹出输入框，允许手动键入单词查询。
* 部分应用（如某些 Electron 或受保护窗口）无法读取选区：提示用户改用「复制后按热键」模式。
* 离线或 API 不可达：命中本地缓存时直接展示缓存结果并标注「缓存于 X 日」；未命中则提示网络异常并允许仅记录单词与上下文，联网后批量补全解析。
* 长文本选中（> 500 字符）：提示「已截断为前 500 字符」并按句子模式处理。
* 多显示器与 200% DPI：浮窗需在正确的显示器上按正确比例渲染。
* Prompt 被用户改坏导致输出非 JSON：自动降级为纯文本展示，并在设置页提示「恢复默认模板」。
* API Key 存储：使用 Windows DPAPI 加密存储，不以明文写入配置文件。

**UI/UX Highlights**

* 浮窗宽度约 420px，最大高度 560px 超出内部滚动；圆角、投影、半透明毛玻璃背景，视觉上不喧宾夺主。
* 信息层级严格：语境释义 > 音标/词性 > 例句 > 联想与搭配，避免「一大坨文本」削弱可读性。
* 键盘全覆盖：Esc 关闭、Enter 收藏、空格朗读、Tab 在分区间跳转，重度用户可全程不碰鼠标。
* 深浅主题均满足 WCAG AA 对比度（正文 ≥ 4.5:1）；音标使用支持 IPA 字形的字体（如 Charis SIL 回退方案）。
* 例句中的目标词高亮，便于快速定位用法。
* 所有加载态使用骨架屏而非转圈，减少等待焦虑。

---

## Narrative

周三晚上十点，李然还在读一篇关于分布式共识的英文论文。他遇到 "the protocol degenerates into a livelock under adversarial scheduling"。他知道每个单词，却拿不准 degenerate 在这里的确切语气，也不确定 livelock 与 deadlock 的区别。

过去他的做法是：切到浏览器，打开词典，得到 "degenerate: 退化，堕落" —— 一个在这个语境里几乎没有帮助的答案；再搜 livelock，翻三个论坛帖子。十分钟后回到论文，思路已经断了。

现在他选中这半句话，按下 Ctrl+Alt+D。不到一秒，浮窗出现：degenerate 在此处是「（系统）退化为、劣化成」，属于技术写作中的中性描述而非贬义；livelock 与 deadlock 的对比被清楚列出——两者都卡住，但 livelock 中进程仍在运行、只是无法推进；接着是三条来自同类技术语境的例句。他点了一下喇叭确认读音，按 Enter 把这两个词收进生词库，打上「distributed-systems」标签，浮窗消失，视线回到论文那一行。整个过程 20 秒，阅读节奏没有断。

三周后他准备组会分享，在 LexNote 里筛选该标签，导出 40 个词到 Anki。对他而言，这是一个终于跟得上阅读速度的工具；对产品而言，这 40 条带上下文的高质量记录，就是他不会再卸载的理由。

---

## Success Metrics

### User-Centric Metrics

* 激活率：完成 LLM 配置并成功查询 ≥ 1 次的安装用户占比，目标 ≥ 40%。
* 核心动作频次：激活用户日均查询次数 ≥ 8 次。
* 收藏转化率：查询后执行收藏的比例 ≥ 30%，反映解析质量被认可。
* 解析满意度：卡片内「有帮助 / 不准确」轻量反馈，正向率 ≥ 85%。
* W1 / W4 留存：分别 ≥ 35% / ≥ 20%。

### Business Metrics

* 上线 8 周安装量 ≥ 3,000。
* 人均生词库沉淀 ≥ 50 条（30 天内）。
* 服务端运营成本维持为 0（不含分发与官网托管）。

### Technical Metrics

* 热键按下到浮窗出现 ≤ 150ms（P95）；首个 token 返回 ≤ 1.5s（P95，依赖用户模型）。
* 取词成功率：主流应用（Chrome、Edge、Acrobat、Word、VS Code、微信）≥ 95%。
* 结构化 JSON 解析成功率 ≥ 98%。
* 崩溃率 < 0.5% 会话；常驻内存占用 < 200MB。

### Tracking Plan

所有埋点仅本地存储，默认关闭上报，用户可选择开启匿名统计。

* app_installed / onboarding_step_completed / llm_config_tested（含成功失败原因）
* hotkey_triggered（含取词方式：UIA / 剪贴板 / 手动输入）
* selection_captured（含内容类型：word / phrase / sentence，字符长度）
* llm_request_sent / llm_first_token / llm_completed / llm_failed（含错误类型、耗时）
* card_rendered / card_section_expanded / tts_played
* word_saved / save_undone / tag_added / duplicate_detected
* library_searched / library_filtered / word_edited / word_reanalyzed / word_deleted
* export_performed（格式、条数）/ import_performed
* feedback_submitted（helpful / inaccurate）

---

## Technical Considerations

### Technical Needs

* 桌面外壳：单一 Windows 桌面应用，包含托盘进程与主窗口；建议采用启动快、内存占用低的原生或轻量 WebView 方案。
* 全局输入层：全局热键注册（RegisterHotKey）、UI Automation 客户端读取选区、剪贴板读写与还原、前台窗口信息获取。
* 浮窗渲染层：非激活式无边框窗口，支持 Per-Monitor DPI Awareness v2。
* LLM 客户端：OpenAI Chat Completions 兼容的 HTTP 客户端，支持 SSE 流式、超时与取消、指数退避重试；结构化输出优先使用 JSON Schema / response_format，对不支持的模型回退为 Prompt 约束 + 容错解析。
* 数据层：本地嵌入式数据库（SQLite），核心表为 words（词条主表）、senses（义项）、examples（例句）、contexts（收藏上下文）、tags / word_tags（标签多对多）、cache（解析缓存）、settings；全文检索使用 FTS5。
* 语音：Windows Speech Synthesis (SAPI / WinRT SpeechSynthesizer)。
* 安全：API Key 经 Windows DPAPI 加密后落盘。

### Integration Points

* 用户自有的 OpenAI 兼容 LLM 服务（云端或本地网关），产品不绑定任何特定供应商。
* Windows 系统能力：全局热键、UI Automation、剪贴板、TTS、托盘、开机自启。
* 导出目标：Anki（TSV 导入）、通用 CSV、Markdown 笔记工具（Obsidian / Logseq）。
* 自动更新渠道（可选）：GitHub Releases 或自建静态更新源。

### Data Storage & Privacy

* 全部用户数据存于本地单一数据库文件，产品不设服务端、不收集任何用户内容。
* 发送给 LLM 的仅包含：选中文本、抓取的上下文句子、系统 Prompt。需在设置页明示这一边界，并提供「关闭上下文抓取」开关以应对敏感场景。
* API Key 加密存储，日志中脱敏；崩溃日志默认不包含用户文本。
* 用户可随时导出全部数据或一键清空数据库，符合数据可携带与被遗忘的基本预期。

### Scalability & Performance

* 单机场景，规模瓶颈在词库容量：需保证 10,000 条词、50,000 条例句下搜索响应 < 100ms（依赖 FTS5 与合理索引）。
* 应用冷启动 ≤ 2s，托盘常驻状态下热键响应不受主窗口是否打开影响。
* 解析缓存降低重复调用；批量补全（离线队列）需限流并支持中断。

### Potential Challenges

* 取词兼容性是最大工程风险：不同应用（Chromium、UWP、Java Swing、受保护 PDF）对 UI Automation 支持差异大，需要建立应用适配清单与稳定的剪贴板回退路径，同时保证剪贴板还原不出错。
* 剪贴板模拟复制可能与用户操作冲突或被安全软件拦截，需要明确的失败提示与手动模式兜底。
* LLM 输出稳定性：不同模型对结构化输出的遵循度差异大，需设计强容错解析器与降级展示，避免弱模型导致卡片崩坏。
* 解析准确性是核心价值主张，Prompt 工程需要持续迭代，建议内置「不准确」反馈样本本地收集，供用户自行调优模板。
* 首次配置门槛：要求用户自备 API Key 会显著折损激活率，需通过预设服务商、清晰错误提示与「获取 Key 指引」链接尽量降低摩擦。

---

## Milestones & Sequencing

### Project Estimate

Large：约 6–8 周至可发布的 V1（含取词兼容性调试与打磨）。

### Team Size & Composition

2 人小团队：

* 1 名全栈桌面工程师（Windows 桌面 + 数据层 + LLM 集成）
* 1 名产品/设计兼职（PRD、Prompt 工程、UI 设计、用户测试），可由发起人本人承担

### Suggested Phases

**Phase 1 — 核心链路打通（Week 1–2）**

* Key Deliverables：工程师交付全局热键 + UI Automation/剪贴板取词 + 浮窗弹出 + OpenAI 兼容流式调用 + 结构化解析卡片渲染（可用但未打磨）。产品交付默认 Prompt 模板 v1 与卡片信息架构。
* Dependencies：确定桌面技术栈；准备至少 2 个测试用 LLM 服务（一云端一本地）验证兼容性。

**Phase 2 — 生词库与数据层（Week 3–4）**

* Key Deliverables：工程师交付 SQLite 数据模型、收藏流程（含去重与撤销）、主窗口词库列表/搜索/筛选/详情编辑、标签体系、解析缓存。产品交付词库交互设计与导出字段映射方案。
* Dependencies：Phase 1 的解析输出 Schema 冻结。

**Phase 3 — 发音、设置与打磨（Week 5–6）**

* Key Deliverables：工程师交付系统 TTS 朗读、设置页（Provider 管理、热键、Prompt 编辑器、主题、数据位置、备份）、导入导出（CSV / Markdown / Anki TSV）、DPI 与多显示器适配、DPAPI 加密。产品完成 Prompt 模板 v2 调优与首次运行引导设计。
* Dependencies：Phase 2 数据层稳定。

**Phase 4 — 兼容性验证与发布（Week 7–8）**

* Key Deliverables：在 Chrome、Edge、Acrobat、Word、VS Code、微信、Kindle 等目标应用逐一验证取词成功率并修复；完成 10–15 人内测、收集解析质量反馈；打包 MSI + 便携版，发布官网与 Release 说明。
* Dependencies：内测用户招募；代码签名证书（如需避免 SmartScreen 拦截）。

**Post-V1 候选（不承诺）**

* 无侵入划词（选中即出图标，无需热键）
* 内建复习与掌握度算法（SM-2 / FSRS）
* 多 Provider 档案与任务级路由（释义用强模型、例句用便宜模型）
* 真人发音音频源接入