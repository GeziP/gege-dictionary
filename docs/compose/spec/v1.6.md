---
feature: v1.6
status: delivered
updated: 2026-09-10
branch: feat/v1.6-review
commits: 26e2769..750d7a1
---

# v1.6 截图 OCR · Anki · 上下文 · 查词窗体验

## Report

**What was built** — 系统级截图 OCR（WinRT + GDI，热键/托盘/框选，结果进既有 Lookup 管道并过 content_filter）；可选 AnkiConnect（强制 loopback，设置页测试连接/牌组，生词详情一键发送与 Front 去重，收藏后可选自动同步）；查词窗可编辑上下文重解析；重用窗口改为事件重置不再 `location.reload()`；空态可跳主窗设置。版本三处 1.6.0，README 与 RELEASE-NOTES 已同步。

**Verification** — `cargo test --lib` **102 passed / 1 ignored**；`npm test` 14 passed；`tsc` / `lint` / `vite build` / `cargo fmt --check` 全 PASS。一审 C1/C2 已修；二审 C3（截断提示不可见）/C4（子窗无法驱动主窗）/C5（saveWord 竞态）已在 `6390a61`+`750d7a1` 修复。

**Journey log**
1. 全屏遮罩内 BitBlt 会截到自己——必须先 `hide()` 再截屏。
2. `spawn_blocking` 里跑 WinRT 必须 `CoInitializeEx(MTA)`。
3. 子窗口 `location.href` 不能驱动主窗路由；需 `show_main_window` + `gege://navigate` 事件。
4. `saveWord` fire-and-forget 时，任何依赖 DB 的后继命令（Anki）都有竞态，必须 await。
5. 窗口 hide 后 setHint 是死代码——用户可见反馈要在 show 状态下给出。

## [S1] Problem

v1.5 后主场景与隐私已稳，但仍缺：扫描件/字幕无法 Ctrl+C；生词进 Anki 仍靠手工 TSV；context≈selection 语境弱；查词窗 reload 闪白、空态不可操作。

完整规格：`docs/PRD-v1.6-spec.md`。

## [S2] Design

### O0 截图 OCR
- 热键默认 `Ctrl+Shift+O`；托盘「截图取词」；全屏遮罩框选。
- 截屏前隐藏遮罩 → GDI BitBlt → 内存 PNG → WinRT OCR（COM MTA）。
- 结果走 `content_filter` + Lookup 管道；`sourceApp=screenshot`。
- 设置：`ocr.enabled` / `hotkey` / `language`；关闭时 unregister 热键。

### O1 Anki Connect
- 默认关；仅 loopback；`enabled=false` 时后端拒绝请求。
- Front=lemma，Back=译文+语境；Extra=explanation（失败回退 Front/Back）。
- 去重：`deck:"x" Front:"lemma"`。

### O2 上下文
- 查词窗可编辑 context → 强制刷新重新解析。

### O3 查词窗
- 重用窗口 `gege-lookup-reset` / CustomEvent，不再 reload。

## [S3] Out of Scope

云 OCR、云 Anki、完整 SRS、Authenticode、双路径合并、多显示器虚拟屏、auto_send 实际行为。

## Tasks

- [x] T1: OCR 引擎封装 + 截屏 — acceptance: 内存位图可出英文文本或明确不可用 (covers: S2)
- [x] T2: 框选窗 + 热键 + 托盘 — acceptance: Ctrl+Shift+O 框选后触发 lookup (covers: S2; depends: T1)
- [x] T3: OCR 接入管道与统计 — acceptance: 过滤/事件与剪贴板同构 (covers: S2; depends: T2)
- [x] T4: AnkiConnect 客户端 + 设置 — acceptance: 测试连接/列牌组 (covers: S2)
- [x] T5: 发送生词与去重 — acceptance: Anki 可见且重复可控 (covers: S2; depends: T4)
- [x] T6: 上下文手编重解析 — acceptance: 改 context 后可重查 (covers: S2)
- [x] T7: 查词窗去 reload — acceptance: 连续查词无 reload 闪白 (covers: S2; depends: T3)
- [x] T8: 版本 1.6.0 + 文档 — acceptance: 三处版本与说明一致 (covers: S2; depends: T1-T7)
