---
feature: v1.7
status: planning
updated: 2026-09-12
branch: TBD
commits: TBD
---

# v1.7 双路径合并 · Anki 闭环 · 上下文与空态

## Report

**What was built** — （交付后填写）

**Verification** — （交付后填写：cargo/npm/tsc/lint/build + 实机清单）

**Journey log**
1. （交付后填写）

## [S1] Problem

v1.6 交付 OCR / Anki / 手编上下文后仍留下：

1. `lookup_word` 与 `lookup_word_stream` 约 150 行重复，审查已出现分叉风险（高技术债）。
2. Anki 无批量发送、字段映射固定、无本地 `anki_note_id` 对账。
3. `captureContext` 仍是布尔；网络/鉴权空态靠英文串匹配；查词窗 reset 可能闪旧词条。

完整规格：`docs/PRD-v1.7-spec.md`。

## [S2] Design

### E0 合并双路径
- `prepare_lookup` 统一 settings/模板/glossary/缓存键/无 Key 拦截。
- 流式与非流仅传输层不同；cache 命中短路并补全 `_templateName`。

### E1 Anki
- 生词库批量「发送到 Anki」。
- `anki.fieldMap` + 可选 Example 字段；Schema v5 `anki_note_id`。

### E2 上下文与空态
- `captureContext`: off / selection_only / surrounding（启发式默认关）。
- 后端错误 `code` 化；四类空态；reset 清 `lookupResult`。

## [S3] Out of Scope

Authenticode（#14）、云同步、完整 SRS、RapidOCR、多语种、常驻钩子。

## Tasks

- [x] T1: prepare_lookup 抽取 + 双路径接线 — acceptance: 流式/非流字段一致，cache hit 有 templateName
- [ ] T2: 对齐单测与主路径回归 — acceptance: cargo + QA A1–A5
  - 仓库内闭环（2026-09-12）：`cargo test --lib` **109 passed / 1 ignored**；`cargo fmt` PASS；vitest 14 PASS；`tsc` / `lint` / `build` PASS。
  - **待实机**：QA 清单 A1–A5 + B/C 组（Anki/OCR/上下文）。
- [x] T3: Anki 批量 UI — SelectionBar「发送到 Anki」+ Toast added/skipped/failed
- [x] T4: 字段映射 — `anki.fieldMap` + examples 独立字段；Basic Front/Back 回退
- [x] T5: Schema v5 noteId — `LATEST=5`，`words.anki_note_id` + JSON `ankiNoteId` 落库；notesInfo 跳过
- [x] T6: captureContext 三档 + 启发式 — off/selection_only/surrounding；`merge_nearby_context` 单测
- [x] T7: 错误 code + 四空态 + reset 清结果 — `[auth|timeout|network|no_key|filtered]`；Lookup reset 调 `clearLookup`
- [x] T8: 版本 1.7.0 + 文档 — package/Cargo/tauri.conf 已同步 1.7.0；RELEASE-NOTES-v1.7.0.md 已写
